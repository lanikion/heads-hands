public class Monster extends Creature {
    public Monster(int attack, int defence, double health, int minDamage, int maxDamage) {
        super(attack, defence, health, minDamage, maxDamage);
    }
}
